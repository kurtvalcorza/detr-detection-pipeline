# Release status

Current status: **Candidate — default-path execution recorded on 2026-09-25 (Kaggle T4); REL12 BYOD exercise pending before promotion**. The pipeline package, the offline test suite (including a tiny random-weight DETR run through fine-tuning, evaluation and adapter reload), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.2) and the standalone `E2E` tutorial `tutorials/detr_detection_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.1) exist. The upstream snapshot is pinned: `MODEL_REVISION` is `1d5f47bd3bdd2c4bbfa585418ffe6da5028b4c0b` and the manifest records every file's byte size and SHA-256 (`model.safetensors` `830f5e2eeaad…`, matching the Hub's LFS record). The exact commit `e39d680` / notebook blob `a98705edf1cb` executed top-to-bottom on Kaggle Tesla T4 on 2026-09-25 UTC with every BYOD gate off (357.5 s, 14/14 post-restart code cells): adapted held-out AP 0.2222 / AP50 0.2668 against 0.0063 / 0.0172 for the re-headed baseline on 10 drawn sign images (17 reference boxes), one seeded split, one runtime; the adapted model returned no detection on any of 3 new drawn images at the default threshold 0.9. The full record is in `docs/release-verification.md`. Release step 7 (REL12: the BYOD dataset branch with a small labelled directory and with an incompatible `annotations.json`) has not been exercised, so the status stays Candidate.
