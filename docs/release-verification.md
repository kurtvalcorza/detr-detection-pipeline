# Release verification

`tutorials/detr_detection_colab.ipynb` (`E2E`, **standalone** carrier) is a **release candidate** until the exact notebook revision has executed top-to-bottom in a clean supported runtime. Unit tests, the tiny-model test, JSON validation, code-cell compilation, the generator parity checks and `tools/validate_release_assets.py` are necessary checks but are **not** runtime evidence under DIMER Notebook Specification 2.1 (REL8). This file is the durable release-gate record for the notebook.

The upstream snapshot is pinned to `1d5f47bd3bdd2c4bbfa585418ffe6da5028b4c0b` and the notebook is regenerated with that revision and manifest. Default-path execution recorded on 2026-09-25 (Kaggle T4); REL12 BYOD exercise pending before promotion: steps 1–6 and 8–9 of the procedure below are covered by the record, step 7 is not, so the status stays `Candidate`.

## Automatic coverage (static and unit, every pull request)

CI installs the pinned CPU-only torch wheel and the other runtime pins, then runs:

- `ruff check src tests tools`;
- `pytest`: snapshot verification and staging against synthetic manifests, the unpinned refusals, the import boundary, input and dataset validation, BYOD directory reading, average precision, the evaluation report, the notebook parity checks, the weight-facts check, and `tests/test_tiny_model.py` — a tiny random-weight DETR taken through `finetune`, `evaluate`, `save_artifact` and `apply_artifact`;
- `tools/validate_release_assets.py`: model card 1.2 structure and front matter, the pin state across the package, the manifest and the documents, identity consistency, the weight facts, the release-status tokens, and the notebook's structure, carried modules, parity, markers, BYOD gates and location fields;
- `tools/build_notebook.py --check`.

These are source, provenance and unit checks. None of them loads the pinned checkpoint, so none of them is execution evidence.

## Supported release verification procedure

Before changing the registry status from `Candidate` to `Release-grade`:

1. confirm the snapshot is pinned (`MODEL_REVISION` is a 40-hex commit and every manifest entry has a SHA-256) and that static CI is green on the exact commit under review;
2. open that exact notebook revision in a new GPU runtime (Colab or Kaggle, Tesla T4 or similar) with **no repository checkout** and a clean model cache;
3. run the notebook top-to-bottom without editing implementation cells, with every form field at its default (`USE_BYOD_IMAGE = False`, `USE_BYOD_DATASET = False`, `threshold = 0.9`, `EPOCHS = 10`);
4. verify that Section 1 reports `NOTEBOOK_SOURCE.repository_revision` equal to `metadata.dimer.generated_from.revision`, and that the installed versions equal the inline `PINS`;
5. verify that every default-path stage completes: the pinned install; the carried modules; staging of all four manifest entries and `verify_snapshot`; `validate_inputs` with the `threshold=1.5` rejection finding; COCO detection and the `sample-sanity` report; the blank and noise probes; `validate_dataset`; the split with its leakage assertion; the baseline; `finetune`; the adapted evaluation; new-data inference; adapter export, reload and the tolerance check; and the six outputs in `outputs/`;
6. record the observed baseline and adapted `ap`, `ap50` and `ap75`, the per-epoch losses, the trainable and total parameter counts, the degenerate-probe counts and the wall time in the table below. No metric value is asserted in advance: a low adapted AP is a finding to record, not a failure by itself;
7. exercise the BYOD dataset branch once with a small labelled directory (`BYOD_DATASET_DIR`) and once with an incompatible `annotations.json`, and record both outcomes (REL12);
8. record the notebook Git blob id, commit, runtime (platform, GPU, Python, PyTorch, Transformers, timm), model identifier and revision, whether the model cache was clean, and any warning judged harmless with the reason;
9. record no access tokens or other secrets.

A known-failing default path in the supported runtime blocks release.

## Recorded executions

Notebook identity is the Git blob id of `tutorials/detr_detection_colab.ipynb` (verify with `git rev-parse <commit>:tutorials/detr_detection_colab.ipynb`). Each record uses the fields Date, Subject, Runtime, Procedure, Observed result and Caveats.

| Date (UTC) | Subject (commit / notebook blob) | Runtime | Procedure | Observed result | Caveats |
|---|---|---|---|---|---|
| 2026-09-25 (23:11:59–23:18:01) | commit `e39d680` / blob `a98705edf1cb` (`NOTEBOOK_SOURCE.repository_revision` `7ee32a4`, the source revision the notebook was generated from; `7ee32a4..e39d680` changes only the notebook and `tests/test_pin_snapshot.py`) | Kaggle Tesla T4 (`kurtvalcorza/dimer-nb2-detr-detection` v1), batch run; image `gcr.io/kaggle-gpu-images/python@sha256:37c64f7dd9c54116ecd1bcc88817c5469b88387388fade02bfa8bf3fc647d461` (image torch 2.10.0+cu128, transformers 5.0.0), Python 3.12.13, Tesla T4 15360 MiB, driver 580.159.04; after the inline pins torch 2.14.0+cu130 (CUDA 13.0), transformers 4.57.6, timm 1.0.29, scipy 1.18.1, numpy 2.5.3, Pillow 11.3.0; device `cuda:0`, float32 | serial suite: blob fetched at the 40-char SHA and Git-blob verified, HF cache clean at start, no repository checkout; top-to-bottom with every form field at its default (`USE_BYOD_IMAGE = False`, `USE_BYOD_DATASET = False`, `threshold = 0.9`, `EPOCHS = 10`); `facebook/detr-resnet-50` at `1d5f47bd3bdd2c4bbfa585418ffe6da5028b4c0b`, 4 manifest files fetched and `verify_snapshot` passed (`verified_files` 4) | **PASSED (default path)** — 357.5 s wall (pass 1 267.4 s stopped after the install cell with `RuntimeError: Core dependencies changed while older modules were loaded` (cuda-bindings 12.9.4 → 13.4.3, numpy 2.0.2 → 2.5.3), kernel restarted after the install cell as the notebook instructs; pass 2 90.0 s), 14/14 post-restart code cells ok; `validate_inputs` recorded the `threshold=1.5` rejection; COCO scene (threshold 0.9): stop sign 0.9991, traffic light 0.9975, clock 0.9733, same-label `box_iou` 0.8465 / 0.9095 / 0.8983, sports ball not detected (0.0), 3/4 matched, `sample-sanity`; blank and noise probes 0 detections at 0.9 and at 0.05; sign dataset 40 records / 82 boxes (stop-sign 21, yield-sign 34, speed-limit-sign 27), `verdict` accepted, split seed 0 → 30 train / 10 held out (17 references); class head re-initialised to 4 slots; `finetune` backbone frozen, 18,047,240 of 41,502,152 parameters trainable, AdamW lr 1e-4, batch 4, 10 epochs, losses 1.6564, 1.0888, 0.9144, 0.8003, 0.7812, 0.7256, 0.6889, 0.6303, 0.5725, 0.5602; held-out evaluation (threshold 0.05, max 100 detections, package-local AP): baseline ap 0.0063 / ap50 0.0172 / ap75 0.0063 (per-class AP50 stop-sign 0.0, yield-sign 0.0218, speed-limit-sign 0.0297; 100 detections per image before the cap), adapted ap 0.2222 / ap50 0.2668 / ap75 0.2479 (per-class AP50 stop-sign 0.2079, yield-sign 0.3140, speed-limit-sign 0.2783; 1–7 detections per image); new-data inference on 3 drawn images (seed 99, 5 truth boxes) at threshold 0.9: **0 detections on all 3**, same-label IoU 0.0 for every truth box; adapter `detr_adapter.safetensors` 72,219,224 B sha256 `6a455a2193082a4d05f1d20edcf53cdf8e7c62c6199ae11a6526eb0796864878` (265 tensors, format `detr-adapter-v1`, base state digest `830f5e2eeaada8c8c8281779dcc8ab12833972eb8514ed0a35be6c1d4420ad81`), reload onto a fresh base: 7 detections compared, tolerance 0.001, equivalent; BYOD refusal probes: non-image → `TypeError`, out-of-bounds box → `ValueError`; six outputs written, sha256 `detr_detection_result.json` `4a18a1b29d82…`, `detr_detection_evaluation_report.json` `b1fde2f1f5a9…`, `detr_detection_input_manifest.json` `e2fcc09d2690…`, `detr_detection_detections.csv` `31a3158b52e0…`, `detr_detection_annotated.png` `b1724b3d2e44…` | One seeded split of 10 drawn images, one runtime, no dispersion estimate; the COCO part is one drawn scene, not a COCO evaluation. The adapted AP is low and the adapted model found nothing on the 3 new images at the default threshold — recorded as findings (step 6), not hidden. Harmless warnings: the checkpoint's unused `num_batches_tracked` backbone buffers, and the expected class-head shape mismatch (92 → 4) when re-heading. **REL12 not exercised** (both BYOD branches off), so the status stays `Candidate`. No tokens or secrets recorded. |
